import { StringListPipe } from './string-list.pipe';

describe('StringListPipe', () => {
  it('create an instance', () => {
    const pipe = new StringListPipe();
    expect(pipe).toBeTruthy();
  });
});
