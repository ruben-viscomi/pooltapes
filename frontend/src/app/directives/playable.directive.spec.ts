import { PlayableDirective } from './playable.directive';

describe('PlayableDirective', () => {
  it('should create an instance', () => {
    const directive = new PlayableDirective();
    expect(directive).toBeTruthy();
  });
});
