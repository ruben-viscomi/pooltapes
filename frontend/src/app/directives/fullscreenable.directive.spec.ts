import { FullscreenableDirective } from './fullscreenable.directive';

describe('FullscreenableDirective', () => {
  it('should create an instance', () => {
    const directive = new FullscreenableDirective();
    expect(directive).toBeTruthy();
  });
});
