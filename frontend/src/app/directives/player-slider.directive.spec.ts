import { PlayerSliderDirective } from './player-slider.directive';

describe('PlayerSliderDirective', () => {
  it('should create an instance', () => {
    const directive = new PlayerSliderDirective();
    expect(directive).toBeTruthy();
  });
});
