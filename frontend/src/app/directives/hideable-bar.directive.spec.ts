import { HideableBarDirective } from './hideable-bar.directive';

describe('HideableBarDirective', () => {
  it('should create an instance', () => {
    const directive = new HideableBarDirective();
    expect(directive).toBeTruthy();
  });
});
