import { ToggleableMenuDirective } from './toggleable-menu.directive';

describe('ToggleableMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleableMenuDirective();
    expect(directive).toBeTruthy();
  });
});
