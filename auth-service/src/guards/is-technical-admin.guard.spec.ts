import { IsTechnicalAdminGuard } from './is-techical-admin.guard';

describe('IsTechnicalAdminGuard', () => {
  it('should be defined', () => {
    expect(new IsTechnicalAdminGuard()).toBeDefined();
  });
});
